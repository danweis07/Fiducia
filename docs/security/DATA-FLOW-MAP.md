# Fiducia Platform — Data Flow Maps

> Visual representation of how data moves through the platform, including authentication, payment, and integration flows.

## 1. High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT TIER                             │
│  React SPA (Vite) ─── CDN (Cloudflare) ─── Mobile (Flutter)    │
└──────────────────────────────┬──────────────────────────────────┘
                               │ HTTPS/TLS 1.2+
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                       EDGE FUNCTION TIER                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │   Gateway     │  │   SSO/OAuth  │  │  Webhooks/Events     │  │
│  │   (RPC Hub)   │  │   Handlers   │  │  Processors          │  │
│  └──────┬───────┘  └──────────────┘  └──────────────────────┘  │
│         │ JWT validation + tenant context                       │
└─────────┼───────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE TIER                              │
│  PostgreSQL (Supabase) ──── RLS enforced on every query         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │
│  │ Banking  │  │ Compliance│  │ Audit    │  │ Control Plane│   │
│  │ Tables   │  │ Tables   │  │ Logs     │  │ (service-only)│  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## 2. Authentication Flow

```
User                   React SPA              Supabase Auth         PostgreSQL
 │                        │                       │                     │
 │── Login (email/pass) ──▶                       │                     │
 │                        │── Auth request ───────▶                     │
 │                        │                       │── Verify creds ────▶│
 │                        │                       │◀── User record ─────│
 │                        │                       │                     │
 │                        │   [If MFA required]   │                     │
 │◀── MFA Challenge ──────│◀── MFA challenge ─────│                     │
 │── TOTP code ───────────▶── Verify TOTP ────────▶                     │
 │                        │                       │                     │
 │                        │◀── JWT + Refresh ─────│                     │
 │◀── Authenticated ──────│                       │                     │
 │                        │                       │                     │
 │── API Request ─────────▶                       │                     │
 │                        │── Gateway call ───────▶── JWT validation ──▶│
 │                        │   + JWT header        │   set firm_id      │
 │                        │                       │── Query (RLS) ─────▶│
 │                        │◀── Response ──────────│◀── Filtered rows ──│
```

## 3. Payment Transfer Flow

```
User            React SPA          Gateway          Approval         Core Banking
 │                 │                  │               Engine           Adapter
 │── Transfer ────▶│                  │                  │               │
 │   request       │── payments. ────▶│                  │               │
 │                 │   transfer       │                  │               │
 │                 │                  │── Check policy ─▶│               │
 │                 │                  │                  │── Threshold   │
 │                 │                  │                  │   check       │
 │                 │                  │                  │               │
 │   [If amount > threshold]         │◀── Requires ────│               │
 │◀── Approval ───│◀── Pending ──────│   approval      │               │
 │   required      │                  │                  │               │
 │                 │                  │                  │               │
 │   [Approver approves]             │                  │               │
 │                 │                  │── Execute ───────│──────────────▶│
 │                 │                  │                  │               │── Process
 │                 │                  │◀─────────────────│◀──────────────│   transfer
 │                 │                  │                  │               │
 │                 │                  │── Audit log ─────▶               │
 │                 │                  │── Notification ──▶ (multi-channel)
 │◀── Confirmed ──│◀── Success ──────│                  │               │
```

## 4. Incident Response & Rollback Flow

```
AlertManager       Gateway          Incidents         Deploy           Notification
 │                   │               Service          Service           Service
 │── Alert fires ───▶│                  │                │                │
 │                   │── incidents. ───▶│                │                │
 │                   │   create         │                │                │
 │                   │                  │── Record ─────▶│ DB             │
 │                   │                  │   incident     │                │
 │                   │                  │                │                │
 │   [Ops initiates rollback]          │                │                │
 │                   │── rollbacks. ───▶│                │                │
 │                   │   initiate       │── Rollback ───▶│                │
 │                   │                  │   request      │── Deploy prev  │
 │                   │                  │                │   version      │
 │                   │                  │◀── Complete ──│                │
 │                   │                  │                │                │
 │                   │── incidents. ───▶│                │                │
 │                   │   notify         │── Send ───────▶│───────────────▶│
 │                   │                  │   alerts       │  Slack/Email   │
 │                   │                  │                │  SMS/Push      │
 │                   │                  │                │                │
 │                   │── Timeline ─────▶│ (append JSONB) │                │
```

## 5. Third-Party Integration Data Flow

```
┌─────────────┐       ┌──────────────┐       ┌──────────────────┐
│  Frontend    │──────▶│  Gateway     │──────▶│  Adapter Layer   │
│  (React)     │◀──────│  (Edge Fn)   │◀──────│                  │
└─────────────┘       └──────────────┘       └────────┬─────────┘
                                                       │
                       ┌───────────────────────────────┼───────────┐
                       │                               │           │
                       ▼                               ▼           ▼
              ┌────────────────┐            ┌──────────────┐  ┌─────────┐
              │ Core Banking   │            │ KYC Provider │  │ Payment │
              │ (Symitar/DNA/  │            │ (Jumio/      │  │ Network │
              │  Corelation)   │            │  Onfido)     │  │ (ACH/   │
              └────────────────┘            └──────────────┘  │  Wire)  │
                                                              └─────────┘

Data Classification:
  ── PII (encrypted in transit + at rest) ──
  ── Financial data (encrypted, audit-logged) ──
  ── Operational data (logged, non-sensitive) ──
```

## 6. Cross-Border Data Flow (Multi-Market)

```
                    ┌─────────────────────────────┐
                    │     Control Plane            │
                    │     (US-East-1)              │
                    │  tenant_registry             │
                    │  tenant_deployment_log       │
                    └──────────┬──────────────────┘
                               │
         ┌─────────────────────┼─────────────────────┐
         │                     │                      │
         ▼                     ▼                      ▼
┌────────────────┐  ┌────────────────┐   ┌────────────────────┐
│  US Region     │  │  EU Region     │   │  APAC Region       │
│  (us-east-1)   │  │  (eu-west-1)   │   │  (ap-south-1)      │
│                │  │                │   │                    │
│  US tenant     │  │  UK/EU tenant  │   │  India tenant      │
│  data stays    │  │  data stays    │   │  data stays        │
│  in US         │  │  in EU (GDPR)  │   │  in India (DPDP)   │
└────────────────┘  └────────────────┘   └────────────────────┘

Data residency enforced per tenant configuration.
Cross-border transfers require explicit consent + adequacy assessment.
```
