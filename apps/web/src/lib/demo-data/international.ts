/**
 * Demo data for international types (forex, cross-border transfers, etc.).
 *
 * Currently no handlers are mapped to these domains in the original demo-data.
 * This file exists as a placeholder for future expansion.
 */

import { ActionHandler } from "./types";

export const internationalHandlers: Record<string, ActionHandler> = {};
